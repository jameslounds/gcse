{
  "name": "src",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.5.0",
    "react-dom": "^16.5.0",
    "react-scripts": "1.1.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "gh-pages -d build"
  },
  "homepage": "https://cucumberbob123.github.io/",
  "devDependencies": {
    "gh-pages": "^1.2.0"
  },
  "proxy": {
    "/api": {
      "target": "http://localhost:5000/",
      "secure": false,
      "disableHostCheck": true
    }
  }
}